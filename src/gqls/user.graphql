scalar EmailScalar
scalar PhoneNumberScalar

type User {
  id: ID!
  username: String!
  firstName: String!
  lastName: String
  fullName: String!
  email: EmailScalar
  phoneNumber: PhoneNumberScalar
}

type Query {
  allUsers: [User!]!
}

type Mutation {
  # Add user-related mutations here if needed
}
